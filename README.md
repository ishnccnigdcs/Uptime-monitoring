# Система мониторинга сайтов

Система для мониторинга доступности сайтов с веб-интерфейсом и уведомлениями в Telegram.

## Возможности

- ✅ Мониторинг сайтов каждую минуту
- 🌐 Веб-интерфейс для управления сайтами
- 📱 Уведомления в Telegram при изменении статуса
- 📊 Статистика и история проверок
- 💾 SQLite база данных для хранения данных

## Установка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### Telegram-уведомления (aiogram)

Уведомления реализованы через `aiogram` в модуле `notifier.py`.

Важно для **Windows + Python 3.14**: `aiogram` зависит от `aiohttp`, а для `cp314` могут отсутствовать готовые колёса (wheel),
из‑за чего установка потребует **Microsoft Visual C++ Build Tools** для сборки `aiohttp`.

Варианты:
- Установить **Build Tools** и затем поставить `aiogram`.
- Или использовать **Python 3.11/3.12**, где установка обычно проходит без сборки.

### 2. Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Узнайте свой Chat ID (можно использовать [@userinfobot](https://t.me/userinfobot))

### 3. Настройка переменных окружения

Создайте файл `.env` или установите переменные окружения:

```bash
# Windows PowerShell
$env:TELEGRAM_BOT_TOKEN="ваш_токен_бота"
$env:TELEGRAM_CHAT_ID="ваш_chat_id"
$env:CHECK_INTERVAL="60"  # Интервал проверки в секундах (по умолчанию 60)

# Linux/Mac
export TELEGRAM_BOT_TOKEN="ваш_токен_бота"
export TELEGRAM_CHAT_ID="ваш_chat_id"
export CHECK_INTERVAL="60"
```

Или создайте файл `start.bat` (Windows) или `start.sh` (Linux) для удобного запуска.

## Запуск

### Запуск веб-интерфейса

```bash
python app.py
```

Веб-интерфейс будет доступен по адресу: http://localhost:5000

### Запуск мониторинга

В отдельном терминале запустите:

```bash
python run_monitor.py
```

Мониторинг будет проверять все добавленные сайты каждую минуту и отправлять уведомления в Telegram при изменении статуса.

## Запуск на виртуальной машине

### Вариант 1: Запуск в фоновом режиме (Linux)

```bash
# Запуск веб-интерфейса в фоне
nohup python app.py > web.log 2>&1 &

# Запуск мониторинга в фоне
nohup python run_monitor.py > monitor.log 2>&1 &
```

### Вариант 2: Использование screen (Linux)

```bash
# Установка screen (если не установлен)
sudo apt-get install screen

# Запуск веб-интерфейса
screen -S web
python app.py
# Нажмите Ctrl+A, затем D для отсоединения

# Запуск мониторинга
screen -S monitor
python run_monitor.py
# Нажмите Ctrl+A, затем D для отсоединения

# Подключение обратно
screen -r web
screen -r monitor
```

### Вариант 3: Использование systemd (Linux)

Создайте файл `/etc/systemd/system/site-monitor-web.service`:

```ini
[Unit]
Description=Site Monitor Web Interface
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/путь/к/проекту
Environment="TELEGRAM_BOT_TOKEN=ваш_токен"
Environment="TELEGRAM_CHAT_ID=ваш_chat_id"
ExecStart=/usr/bin/python3 /путь/к/проекту/app.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Создайте файл `/etc/systemd/system/site-monitor.service`:

```ini
[Unit]
Description=Site Monitor
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/путь/к/проекту
Environment="TELEGRAM_BOT_TOKEN=ваш_токен"
Environment="TELEGRAM_CHAT_ID=ваш_chat_id"
Environment="CHECK_INTERVAL=60"
ExecStart=/usr/bin/python3 /путь/к/проекту/run_monitor.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
sudo systemctl daemon-reload
sudo systemctl enable site-monitor-web
sudo systemctl enable site-monitor
sudo systemctl start site-monitor-web
sudo systemctl start site-monitor
```

### Вариант 4: Windows Task Scheduler

Создайте два bat-файла:

`start_web.bat`:
```batch
@echo off
cd /d "C:\путь\к\проекту"
set TELEGRAM_BOT_TOKEN=ваш_токен
set TELEGRAM_CHAT_ID=ваш_chat_id
python app.py
```

`start_monitor.bat`:
```batch
@echo off
cd /d "C:\путь\к\проекту"
set TELEGRAM_BOT_TOKEN=ваш_токен
set TELEGRAM_CHAT_ID=ваш_chat_id
set CHECK_INTERVAL=60
python run_monitor.py
```

Настройте Task Scheduler для автоматического запуска при загрузке системы.

## Структура проекта

```
.
├── app.py              # Flask веб-приложение
├── monitor.py          # Модуль мониторинга сайтов
├── notifier.py        # Telegram уведомления (aiogram)
├── run_monitor.py      # Скрипт запуска мониторинга
├── requirements.txt    # Зависимости Python
├── monitoring.db       # SQLite база данных (создается автоматически)
├── templates/
│   └── index.html     # HTML шаблон
└── static/
    ├── css/
    │   └── style.css  # Стили
    └── js/
        └── app.js     # JavaScript код
```

## API Endpoints

- `GET /` - Главная страница
- `GET /api/sites` - Получить список всех сайтов
- `POST /api/sites` - Добавить новый сайт
- `DELETE /api/sites/<id>` - Удалить сайт
- `POST /api/sites/<id>/check` - Проверить сайт немедленно
- `GET /api/stats` - Получить статистику

## Использование

1. Откройте веб-интерфейс в браузере
2. Нажмите "Добавить сайт" и введите URL
3. Сайт автоматически начнет мониториться каждую минуту
4. При изменении статуса вы получите уведомление в Telegram

## Примечания

- Система проверяет сайты каждую минуту (интервал можно изменить через переменную окружения CHECK_INTERVAL)
- Уведомления отправляются только при изменении статуса (доступен → недоступен или наоборот)
- База данных SQLite создается автоматически при первом запуске
- Для работы на виртуальной машине убедитесь, что порт 5000 открыт в файрволе
